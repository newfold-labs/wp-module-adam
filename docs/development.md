# Development

PHP: `composer run lint`, `composer run fix`. i18n: `composer i18n`. When changing dependencies, update [dependencies.md](dependencies.md). When cutting a release, update **docs/changelog.md**.
